Context下有两个子类，ContextWrapper是上下文功能的封装类，而ContextImpl则是上下文功能的实现类

ContextWrapper有三个直接的子类， ContextThemeWrapper、Service和Application。其中，
ContextThemeWrapper是一个带主题的封装类，而它有一个直接子类就是Activity

startActivity(),diolog
一个Activity的启动必须要建立在另一个Activity的基础之上，也就是以此形成的返回栈,Activity类型的context

Context数量 = Activity数量 + Service数量 + 1 （1为Application）

PhoneWindow有一个ViewRoot，通过addView添加View


跨进程通信方式：文件、广播、aidl、messenger